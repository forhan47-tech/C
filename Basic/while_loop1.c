#include <stdio.h>

int main() {
    int num = 1; // Initialize variable

    while (num <= 5) { // Condition
        printf("%d\n", num); // Print the value of num
        num++; // Increment num
    }

    return 0;
}